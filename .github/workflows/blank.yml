name: Scheduled weekday job

on:
  schedule:
    - cron: "0 10 * * 1-5"  # Lunes a viernes a las 10:00 (UTC)

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run a sample command
        run: echo "Ejecución programada de lunes a viernes a las 10:00 (UTC)"

